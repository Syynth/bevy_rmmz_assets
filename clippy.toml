# Allow unwrap/expect/panic/dbg/println in test code.
# Applies to #[cfg(test)] modules and #[test] functions only.
allow-unwrap-in-tests = true
allow-expect-in-tests = true
allow-dbg-in-tests = true
allow-print-in-tests = true
